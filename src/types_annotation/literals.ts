// Estou definindo que o tipo de x number pois 10 é number
let x=10
// Não irá permitir pois x é number
// x='asd'

// O tipo de Y é 10, pois como é uma const não irá atribuir o y a number, sendo então um tipo literal.
const y=10

// Mesma coisa serve para o type annotation
let a:100=100
// Como foi definido que a é do tipo 100 só irá ser 100

// a=120


let people3={
    // NAME IRÁ SER COMO UMA CONST, NÃO SENDO POSSÍVEL SER ALTERADO
    name:'MARK ' as const,
    age:14
}
export default 1
